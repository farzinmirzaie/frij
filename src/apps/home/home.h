#ifndef FRIJ_HOME_H
#define FRIJ_HOME_H

#include "app.h"

// Home — the default landing tile: a simple watch face (time + date).
// Register it FIRST so it's the launcher's first glance.
const frij_app_t* home_app(void);

#endif  // FRIJ_HOME_H
