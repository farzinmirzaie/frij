#ifndef FRIJ_STOPWATCH_H
#define FRIJ_STOPWATCH_H

#include "app.h"

// Returns the Stopwatch app descriptor (lives for the whole program).
const frij_app_t* stopwatch_app(void);

#endif  // FRIJ_STOPWATCH_H
